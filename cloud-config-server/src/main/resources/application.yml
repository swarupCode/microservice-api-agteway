server:
  port: 9292

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/swarupCode/config-server
          clone-on-start: true


